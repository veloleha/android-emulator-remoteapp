
  # Phone Emulator Hub Prototype

  This is a code bundle for Phone Emulator Hub Prototype. The original project is available at https://www.figma.com/design/frRCElIAEaY4mBBFIBkzTt/Phone-Emulator-Hub-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  